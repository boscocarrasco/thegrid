# Does an accessibility-derived observation layer beat a screenshot loop?

**Short answer: yes under an equal budget, no when tokens are free.** Given
the same enforced budget of 8,000 fresh tokens per task, crop-priority (B)
completes **27/27 tasks against the baseline's 17/27** — intervals do not
overlap, and 16 of the baseline's 27 runs ran out of budget mid-task. With
tokens unconstrained the two are indistinguishable (0.880 vs 0.900).

The tool also makes visual grounding failure structurally impossible (0.26 →
0.00 failures per task), cuts image tokens ~6.4×, and holds a 91 %
prefix-cache hit rate against the baseline's 21.5 %, worth ~366 ms/step of
time-to-first-token. It does not reduce the number of model calls.

Two results deserve to be read before anything else:

- **Crop-priority (B) beats text-priority (C) exactly where the design says it
  should: 8/8 versus 1/8.** When the decisive fact appears mid-task and is
  carried only by appearance, the text-only delta is blind to it — and it
  costs B only 110 image tokens per task to see it. Under the enforced budget
  B also beats C outright, 27/27 against 21/27.
- **"Only send what changed" is the load-bearing part, not the element table.**
  Arm D (table + full screenshot, no deltas) matches the baseline's success
  rate at **2.6× the cost per completed task** and 3.1× arm B's, making it
  the worst arm measured.

All figures come from `RESULTS.md`, generated by `analysis/aggregate.py` from
`results/raw/*.jsonl`. 199 runs plus an 81-run enforced-budget sweep, 0
excluded. Intervals are 95 % bootstrap
percentile intervals over runs.

---

## 1. What was measured

Four observation channels, everything else held constant — same model (Claude
Sonnet), same system-prompt skeleton, same tasks, same step limits, same
executor, same debounce, same seeds, same deterministic reset, arms
interleaved so any drift hit all of them equally. `arms/channels.py` is the
entire independent variable.

| Arm | Start of task | Each step | Action |
|---|---|---|---|
| **A** baseline | task + full screenshot | full screenshot, **replacing** the previous one | pixel coordinates |
| **B** crop-priority | task + full element table + anchor screenshot | verb delta + labelled pixel crop of every changed region | element id |
| **C** text-priority | same as B | verb delta, text only; crop only where the tree cannot describe the change | element id |
| **D** ablation | same as B | full table + full screenshot, **no deltas** | element id |

16 tasks × 4 arms × 3–4 repetitions = **199 runs**, plus a separate
**81-run** sweep with a token budget enforced during each run (§2.4d). Every
task ends in a programmatic check of final state — file bytes, CSV contents,
page state read over CDP. No model judged success anywhere.

Total measured API spend: **$16.83** of the $40 budget.

---

## 2. Results

### 2.1 Headline

| | A (baseline) | B (crop-priority) | C (text-priority) | D (table+shot) |
|---|---|---|---|---|
| Success rate | 0.900 [0.820, 0.980] | 0.880 [0.780, 0.960] | 0.760 [0.640, 0.880] | 0.878 [0.776, 0.959] |
| Steps / task | 6.9 [6.0, 8.0] | 8.9 [7.7, 10.2] | 9.7 [8.3, 11.1] | 8.2 [7.0, 9.6] |
| Model calls / completed task | 7.7 | 10.1 | 12.7 | 9.3 |
| Cost / completed task (USD) | 0.0627 | **0.0513** | 0.0834 | 0.1616 |
| Total tokens / task | **16,858** | 66,712 | 83,427 | 179,969 |

With tokens unconstrained, **all four success rates overlap** except that C is
nominally lowest. On the design document's primary metric — model calls per
completed task — the baseline is nominally best. Neither difference is
detectable at this N.

That is a negative result on the central claim as usually stated, and §2.4
shows why it is also the wrong way to pose the question.

### 2.2 Where the tool wins decisively

Three effects separate cleanly.

**Grounding failure is eliminated, not reduced.**

| | A | B | C | D |
|---|---|---|---|---|
| Grounding failures / task | 0.26 [0.08, 0.48] | **0.00** | **0.00** | **0.00** |
| Runs with ≥1 grounding failure | 12.0 % | **0 %** | **0 %** | **0 %** |

A grounding failure here is a click that landed on no element at all. For the
id-addressed arms this is not merely rare but impossible by construction: the
agent picks an id and the coordinates come from the operating system. The
design document's claim — *"ese error no se reduce: deja de ser posible"* — is
confirmed exactly.

The act-time validation counter shows the other side of the same coin: C and D
abort 0.10 and 0.12 actions per task respectively, catching cases where the
element moved or vanished between the model deciding and the executor acting.
Those are failures the baseline cannot even detect; it just clicks.

**Image tokens drop ~6.5×.**

| | A | B | C | D |
|---|---|---|---|---|
| Image tokens / task | 8,522 [7,343, 9,849] | 1,325 [1,299, 1,353] | 1,278 [1,230, 1,347] | 10,075 [8,546, 11,729] |

**The append-only design does what it claims to the cache.**

| | A | B | C | D |
|---|---|---|---|---|
| Cache hit share | 21.5 % | **90.9 %** | **91.0 %** | 85.6 % |
| TTFT / step (ms) | 1,285 [1,222, 1,353] | **919 [876, 962]** | **915 [867, 966]** | 1,000 [951, 1,052] |

The baseline replaces its screenshot each step, which invalidates the prefix;
the tool arms append and reuse it. That shows up directly as ~366 ms/step less
time-to-first-token, separated. The append-only design is validated on its own
terms.

### 2.3 The surprise: raw token count runs the other way

The baseline uses **4.0× fewer total tokens** than B and 4.9× fewer than C —
separated, and the opposite of the design document's prediction (~15 k for the
tool vs ~50 k for the standard loop).

The mechanism: the tool arms pay a large one-off element table and then never
discard it. An append-only context is re-read in full every step, so cache-read
tokens accumulate roughly quadratically in step count (B: 59,801 cache-read
tokens per task). The baseline's rebuilt context stays short — one screenshot
plus a text history — so its total stays flat.

This does *not* translate into a 4× cost disadvantage, because cache reads bill
at roughly a tenth of fresh input: B is in fact the **cheapest** arm per
completed task ($0.0513 vs A's $0.0627). But it means the compaction and
crop-ageing the design document specifies are not optional niceties — they are
the difference between the delta mechanism paying off and quietly not. Tasks
here were too short (7–10 steps) to trigger either, so neither was exercised.

### 2.4 Equal budget: the answer depends on what you hold equal

The brief asks for success at equal token budget. Running it three ways shows
the conclusion inverting with the measure — which is itself the finding.

**(a) Budget on raw total tokens — baseline wins**

| Budget | A | B | C | D |
|---|---|---|---|---|
| 15,000 | **64.0 %** | 24.0 % | 28.0 % | 0.0 % |
| 30,000 | **90.0 %** | 50.0 % | 36.0 % | 30.6 % |
| 60,000 | **90.0 %** | 72.0 % | 56.0 % | 51.0 % |

**(b) Budget on fresh tokens (input + cache-write + output) — B wins**

| Budget | A | B | C | D |
|---|---|---|---|---|
| 4,000 | 18.0 % | **46.0 %** | 42.0 % | 0.0 % |
| 8,000 | 32.0 % | **68.0 %** | 56.0 % | 30.6 % |
| 15,000 | 72.0 % | **80.0 %** | 66.0 % | 53.1 % |
| 30,000 | **90.0 %** | 88.0 % | 76.0 % | 73.5 % |

**(c) Budget on money — B wins at tight budgets, the baseline takes over as they loosen**

| Budget | A | B | C | D |
|---|---|---|---|---|
| $0.02 | 18.0 % | **44.0 %** | 38.0 % | 0.0 % |
| $0.04 | 44.0 % | **64.0 %** | 54.0 % | 30.6 % |
| $0.06 | **72.0 %** | 70.0 % | 60.0 % | 46.9 % |
| $0.10 | **90.0 %** | 80.0 % | 66.0 % | 67.3 % |

At $0.02 per task the tool completes 2.4× as many tasks as the baseline; by
$0.06 the baseline is level, and by $0.10 it is ten points ahead. Budgeting on raw tokens
reverses the finding entirely, because it charges the append-only arms full
price for tokens the provider served from cache. Anyone reporting a single
equal-budget number here is choosing their conclusion by choosing their
denominator.

**(d) Budget enforced during the run — the version that actually settles it**

Tables (a)–(c) reclassify completed runs. This is a separate 81-run sweep in
which the ceiling was enforced *while each run executed*, with every task's
step limit doubled so an arm with budget to spare could convert it into extra
steps rather than being capped by steps instead.

| Arm | n | Success rate | Runs stopped by the budget | Steps / task |
|---|---|---|---|---|
| A | 27 | 0.630 [0.444, 0.815] | **16/27** | 4.6 [4.3, 4.9] |
| **B** | 27 | **1.000 [1.000, 1.000]** | **0/27** | 6.1 [5.4, 6.9] |
| C | 27 | 0.778 [0.630, 0.926] | 6/27 | 6.5 [5.6, 7.5] |

Ceiling: 8,000 fresh tokens per task. **B separates from both A and C** — its
interval overlaps neither. The baseline ran out of budget in 16 of 27 runs,
usually around step 4–5, because each step costs it a fresh full screenshot
with no cache relief. B never hit the ceiling at all and used its headroom to
take more steps (6.1 vs 4.6).

This is the comparison the brief asks for, and it is the clearest answer the
experiment produces: **at equal budget the tool wins, and it wins because the
baseline cannot afford to keep looking.**

### 2.5 B versus C: crops earn their keep, but only where appearance carries meaning

Pooled over all 16 tasks, B and C overlap on every metric (success 0.880 vs
0.760). Split by task type, the picture is completely different:

| Group | Arm | n | Success | Image tokens | Steps |
|---|---|---|---|---|---|
| **mid-task appearance change** | A | 8 | 1.000 | 6,140 | 5.0 |
| | **B** | 8 | **1.000** | **1,338** | **5.0** |
| | **C** | 8 | **0.125** | 1,228 | 11.5 |
| | D | 7 | 1.000 | 6,140 | 5.0 |
| non-textual but anchor-solvable | A | 9 | 1.000 | 4,912 | 4.0 |
| | B | 9 | 1.000 | 1,228 | 4.0 |
| | C | 9 | 1.000 | 1,228 | 4.0 |
| everything else | A | 33 | 0.848 | 10,084 | 8.2 |
| | B | 33 | 0.818 | 1,348 | 11.2 |
| | C | 33 | 0.848 | 1,303 | 10.8 |

The **mid-task appearance change** group (`vdelta_rows`, `vdelta_bars`) is the
one that tests the question. Both tasks put the decisive fact in an element
that *appears during the task* and whose meaning is carried only by
appearance: six list rows with ordinary names where one has a red background,
and five labelled bars where only pixel width says which is longest. The tree
reports the names and says nothing about colour or width.

- **B crops all six added rows and scores 8/8**, at a cost of 110 image tokens
  per task over C (1,338 vs 1,228, i.e. +9 % on this group and 1.8 % of
  what arm A spends on the same tasks).
- **C receives `+ added list item "job-delta"` and five identical siblings,
  and scores 1/8** — the one success is consistent with guessing among six.
  It also burns 11.5 steps against B's 5.0, clicking around trying to discover
  by interaction what a single crop would have shown.

This is the sharpest result in the experiment, and it is the design document's
own prediction: crop-priority costs +3.4 % image tokens against text-priority across
the whole suite and buys the ability to see anything the tree cannot describe.

The first three non-textual tasks I built (bar chart, filled-circle count,
colour badges) failed to test this at all: every arm scored 3/3 in 4 steps
because all three are answerable from the anchor screenshot alone, so arm B
emitted **zero** crops on them. That was a flaw in my task design, found by
inspecting why the groups were identical, and fixed by adding the two tasks
above. Both groups are reported separately rather than pooled.

### 2.6 Arm D: the table alone is not the mechanism

Arm D isolates "having the element table" from "sending only what changed". It
gets the full table *and* a full screenshot every step, with no deltas.

| | A | B | D |
|---|---|---|---|
| Success rate | 0.900 | 0.880 | 0.878 |
| Total tokens / task | 16,858 | 66,712 | **179,969** |
| Cost / completed task | $0.0627 | **$0.0513** | **$0.1616** |
| Grounding failures | 0.26 | 0.00 | 0.00 |

D is level with the baseline's success rate at **2.6× the cost per completed
task** and 3.1× B's — the most expensive arm by a wide margin. It inherits the table's
benefit (zero grounding failures) but, by re-sending the whole table and a
whole screenshot every step, it inherits neither the baseline's short context
nor the delta arms' small per-step payload.

D also produced the experiment's one interesting outlier: it is the only arm
that ever completed `cross_report_summary` (1/3 against 0/3 for A, B and C),
the hardest cross-application task. Having both the table and the picture
simultaneously appears to help on genuinely hard navigation — at a price no
one should want to pay routinely.

### 2.7 Task-level notes

One task fails on all four arms and contributes only noise:

- `cross_report_summary` — 1/12 (the single D success). Every other run hit
  the 18-step limit.

**`calc_total` was a second such task until the verifier itself was found to be
wrong.** It searched the saved CSV for the string `48.5`; the total the task
asks for is 1.5 + 3.25 + 7.0 = **11.75**, which no correct answer could ever
satisfy. Eleven of twelve runs wrote the right total, saved it as CSV exactly
as instructed, and were recorded as failures. The one genuine failure is a D
run that never created the file at all. The bug and the correction are set out
in DECISIONS D6.1; the corrected task scores A 3/3, B 3/3, C 3/3, D 2/3.

The correction was worth making rather than noting, because the earlier reading
of this task was not merely imprecise but backwards: previous versions of this
report explained the 0/12 as LibreOffice's Save-As dialog defeating every arm.
The agents cleared that dialog on eleven of twelve attempts. A verifier that
cannot be satisfied does not measure difficulty, it manufactures it — and it
took looking at what the runs actually wrote, not at their scores, to see it.

`files_rename` is the one task where the baseline beat the tool arms outright:
A 3/3 in 6.0 steps, B/C/D 2/3 in ~16 steps. All three tool arms exhausted
their step budget in the file manager's context menu, which suggests pcmanfm's
popup-menu representation in the tree is harder to navigate than a picture of
it. Not investigated further.

Both failing tasks are retained. Dropping tasks after seeing which ones failed
is how results get manufactured.

### 2.8 Long tasks: compaction, and where the tool actually pays off

The first round ran 7–10 step tasks, so compaction and crop ageing — the two
mechanisms that bound an append-only context — never fired, and the tool arms
were measured without the remedy for their main weakness. Both are now
implemented and exercised on four tasks with a 40-step limit (17 steps mean).

**Compaction fires as designed.** In 12/12 runs for each tool arm, 1.5–1.6
times per task, at natural boundaries (application change, dialog opening or
closing) and on the re-anchor interval. Arm B emitted 13.6 crops per task and
aged 6.8 of them out into text descriptions; arm C emitted 6.5 and aged 2.8.

**On long tasks the tool wins outright, which it did not on short ones:**

| Long tasks only | A | B | C | D |
|---|---|---|---|---|
| Success rate | 0.917 [0.750, 1.000] | **1.000** | **1.000** | **1.000** |
| Cost / completed task | $0.1814 | **$0.0775** | **$0.0766** | $0.1727 |
| Cost / task | 0.1663 [0.129, 0.217] | **0.0787 [0.051, 0.108]** | 0.0791 [0.050, 0.111] | 0.1727 |
| Cache hit share | 10.0 % | 85.2 % | 85.7 % | 78.4 % |

The cost intervals for A and B do not overlap: on tasks of this length the
tool is **2.3× cheaper per completed task** and finishes everything the
baseline leaves unfinished. The baseline's cache hit rate collapses to 10 %
because it rebuilds its transcript every step, and at 17 steps that is 17 full
screenshots paid for at fresh-input rates.

**But compaction does not save money — it only bounds the context.** Running
the same arms and tasks with compaction switched off:

| | tokens / task | cost / task |
|---|---|---|
| B, compaction **on** | 86,885 | $0.0787 [0.051, 0.108] |
| B, compaction **off** | 151,945 | $0.0763 [0.045, 0.115] |
| C, compaction **on** | 88,545 | $0.0791 [0.050, 0.111] |
| C, compaction **off** | 142,708 | $0.0741 [0.047, 0.105] |

Compaction cuts total tokens **43 %** for B and 38 % for C, and changes cost by
nothing detectable — if anything it is marginally *more* expensive. The reason
is visible in the cache column: compacting rebuilds the prefix, so the tokens
it saves on cheap cache reads come back as expensive cache writes. Its value
at these lengths is keeping the context bounded and the per-step latency flat,
not reducing the bill. Whether it pays for itself at 50 or 100 steps, where an
uncompacted context would eventually stop fitting at all, is not something
17-step tasks can answer.


### 2.9 Token anatomy: why each arm spends what it spends

The headline table poses two puzzles it does not answer. Arm C sends a
*strictly smaller* per-step payload than B — it emits no crops — yet spends
12 % more total tokens. Arm A sends a full screenshot every single step, yet
spends 4× fewer total tokens than B. `analysis/token_anatomy.py` decomposes
both; full output in `results/token_anatomy.txt`.

**Where the tokens actually sit** (per task, 271 runs):

| Arm | total | cache-read | cache-write | input | output | read share |
|---|---|---|---|---|---|---|
| A | 22,144 | 3,687 | 17,916 | 18 | 523 | 16.7 % |
| B | 83,805 | 75,124 | 7,664 | 23 | 994 | 89.6 % |
| C | 93,870 | 83,629 | 8,547 | 24 | 1,669 | 89.1 % |
| D | 176,599 | 148,178 | 27,634 | 19 | 768 | 83.9 % |

#### Why A spends fewer tokens than B

The gap between them is 61,661 tokens per task. The gap in **cache reads
alone is 71,437** — 116 % of it (over 100 % because A more than makes up the
difference in cache *writes*, where it spends 2.3× what B does). The entire
difference is re-read tokens, and nothing else.

The mechanism is the transcript. Arm A rebuilds its conversation every step in
order to drop the previous screenshot, so it never re-reads a prefix: its
total is `sum of per-step payloads`, linear in step count, and the pooled fit
confirms it almost exactly (R² = 0.999 linear). Arm B appends, so step *k*
re-reads everything steps 1…*k*−1 put there: its total is `sum of prefixes`,
which grows faster than linearly. Fitting within a single task, where the
element table size is held fixed, a quadratic term buys B a further +0.20 of
R² (0.677 → 0.874).

**But cheaper in tokens is dearer in money.** Those re-read tokens bill at
roughly a tenth of fresh input, so the ranking inverts the moment you count
what the provider actually had to process:

| | A | B | A/B |
|---|---|---|---|
| Total tokens / task | 22,144 | 83,805 | **0.26×** |
| Fresh tokens / task (input + cache-write + output) | 18,457 | 8,681 | **2.13×** |
| Cost / task | $0.0777 | $0.0557 | **1.40×** |
| Effective price per 100 k tokens | $0.3507 | $0.0664 | **5.3×** |

Arm A pays 5.3× more per token than B because almost none of its tokens are
cached. "A uses fewer tokens" is an artefact of counting a cache read the same
as a fresh one; what A actually does is trade many cheap tokens for fewer
expensive ones, and lose on the exchange.

#### Why C spends more than B

Not because of its channel — because of what happens when it cannot see.

Splitting the 74 runs of each arm by task group:

| Group | runs | B tokens | C tokens | B steps | C steps | B success | C success |
|---|---|---|---|---|---|---|---|
| mid-task appearance change | 8 | **21,159** | **130,217** | 5.0 | 11.5 | 1.000 | 0.125 |
| everything else | 66 | 91,399 | **89,464** | 12.4 | 12.3 | 0.909 | 0.924 |

Outside the group where C is blind, **C is the cheaper arm** — 1,934 tokens
per task less than B, which is exactly what a channel that omits crops should
cost. That group of 8 runs accounts for **119 % of C's total excess**.

Inside it, C spends 6.2× what B spends, takes 2.3× the steps, and still fails.
It cannot tell six identically-named rows apart, so it does the only thing left
open to it: clicks them one by one, and every extra step adds a turn that every
later step re-reads. Its output tokens per step run 1.89× B's pooled — but only
1.04× when matched task-for-task and step-for-step, which localises the extra
reasoning to precisely those runs.

The exchange rate is the point:

> In that group arm B spent **110 extra image tokens** per task on crops, and
> spent **109,058 fewer tokens overall**. Each token of crop returned roughly
> **990 tokens** — and the difference between finishing and not.

The general form: a delta channel that omits information the agent needs does
not save the tokens it withheld. It pays them back with interest, because the
agent spends extra turns recovering the information, and in an append-only
context every extra turn is re-read by every turn after it.


---

## 3. The three questions, answered

### Does the tool (B or C) beat the baseline (A)? On which metric, and by how much?

**On grounding, pixels, cache and cost-constrained throughput: yes,
decisively. On unconstrained success rate and model calls: no.**

| Metric | A | best tool arm | verdict |
|---|---|---|---|
| Grounding failures / task | 0.26 | **0.00** (B, C, D) | separated — eliminated |
| Image tokens / task | 8,522 | **1,278** (C) | separated — 6.7× |
| TTFT / step | 1,285 ms | **915 ms** (C) | separated — 1.40× |
| Cache hit share | 21.5 % | **91.0 %** (C) | separated |
| Cost / completed task | $0.0627 | **$0.0513** (B) | overlapping |
| Success @ 8k fresh tokens (enforced) | 0.630 | **1.000** (B) | separated — 1.59× |
| Success @ $0.02/task | 18.0 % | **44.0 %** (B) | 2.4× |
| Success (unconstrained) | 0.900 | 0.880 (B) | no difference detectable |
| Model calls / task | **6.9** | 8.9 (B) | no difference detectable |
| Total tokens / task | **16,858** | 66,712 (B) | separated — against the tool |

The honest summary: the tool changes *how* the agent fails and *what it costs
per unit of progress*. Give both arms all the tokens they want and they are
indistinguishable; give both the same budget and the tool finishes everything
while the baseline runs dry two-thirds of the way through 16 of 27 tasks.

### Does B beat C — do the crops earn their extra tokens?

**Yes, and the margin is decisive where it matters.** On the task class where
the decisive information is a mid-task appearance change, **B scores 8/8 and C
scores 1/8**, with C also taking 2.3× the steps. Under the enforced equal
budget B beats C 27/27 against 21/27, intervals not overlapping. The extra cost of crop-priority
is 110 image tokens per task (1,338 vs 1,228): +9 % on this group,
+3.4 % across the whole suite.

Pooled across all tasks the two overlap (0.880 vs 0.760), because 14 of 16
tasks are dominated by textual changes the tree already describes literally —
precisely the one case the specification says needs no crop, where B correctly
emits nothing extra.

So the answer is conditional and cheap to act on: crops cost almost nothing on
ordinary tasks (+3.4 % image tokens suite-wide) because they rarely fire, and they are the difference between
working and not working when appearance carries meaning. Send them.

### Where does the advantage come from — the table, the deltas, or the cache?

Now separable, with arm D run.

- **The table is what eliminates grounding failure.** Every id-addressed arm
  (B, C, D) records exactly 0.00 grounding failures against the baseline's
  0.26. The delta mechanism cannot explain this — it does not touch how an
  action is addressed.
- **The deltas are what make it affordable.** D has the table and no deltas,
  and costs $0.1616 per completed task against B's $0.0513 — a **3.1×
  penalty** for that one difference. The table alone is not a win; the table
  plus "send only what changed" is.
- **The cache is real but secondary.** 91 % hit rate for B/C versus 21.5 % for
  A, worth ~366 ms/step of TTFT. It is a latency benefit, clearly attributable
  to the append-only design; but D also achieves 85.7 % and is still the most
  expensive arm, so cache retention alone does not deliver the economics.

Ranked: **deltas ≫ table > cache**, with the table supplying correctness
(grounding) and the deltas supplying economics.

---

## 4. Threats to validity

**N is small.** 3–4 repetitions per (task, arm), ~50 runs per arm. Most pooled
comparisons return "no difference detectable", which means *underpowered*, not
*equal*: a success-rate gap under ~15 points could not be detected. The B-vs-C
result in §2.5 is exempt from this concern only because it is 8/8 against 1/8
— a near-complete separation — but it rests on **two tasks**, and two tasks is
a narrow base for a general claim.

**The vdelta tasks were designed after seeing that the first visual tasks
failed to discriminate.** They were built to make the B/C difference visible,
and they succeed at that by construction. They demonstrate that a real,
plausible task class exists where crop-priority is decisive; they do not
establish how common that class is in real work. That frequency is what
determines whether crops matter in practice, and it was not measured.

**One of sixteen tasks fails almost universally** (`cross_report_summary`
1/12), so ~6 % of the suite depresses all arms equally and contributes no
discriminating power.

**A verifier was wrong, and every success rate here is the corrected one.**
`calc_total` was scored against a total no correct answer produces, so eleven
runs that did the task properly were recorded as failures (DECISIONS D6.1).
The bug hit all four arms in proportion, so no comparison in this report
changes direction — but every absolute success rate rose by 4–6 points when it
was fixed, and earlier versions of this report published the wrong ones. The
correction is applied by `analysis/rescore.py` at analysis time, from the file
contents the broken verifier preserved inside its own failure message; the raw
JSONL is unedited and `--no-rescore` reproduces the original figures. What
should worry a reader is not this bug but the class it belongs to: until it was
found, nothing in the harness checked that a verifier could be satisfied at
all, so a second one like it would have looked exactly like a hard task.

**The enforced equal-budget sweep uses one ceiling on a 9-task subset.** It
was run at 8,000 fresh tokens per task over 81 runs; a different ceiling or a
different task mix could move it, and no ceiling sweep was done. Tables (a)–(c)
in §2.4 remain post-hoc reclassifications and should be read as such.

**Temperature is not pinned.** The CLI exposes no temperature control, so
decoding settings are identical across arms by construction but are not 0.
This widens every interval and makes the underpowering worse.

**The model channel is the `claude` CLI, not the raw API.** Every call carries
~1.1 k tokens of residual harness prompt — identical across arms, so it cannot
bias the comparison, but it inflates absolute figures and compresses relative
differences by adding a constant to both sides of every ratio.

**Arm A was reimplemented mid-experiment and its first 14 runs discarded.**
The first version accumulated screenshots in an append-only context, giving
the baseline caching the standard loop does not have. Those runs are archived
in `results/raw/discarded_armA_v1.jsonl` and excluded from every figure. If my
corrected baseline is wrong in some way I have not seen, every A figure moves.
Its history is textual: a baseline retaining the last 2–3 screenshots would be
a different and arguably stronger configuration, and was not measured.

**Compaction is exercised but only to ~17 steps.** §2.8 shows it firing 1.5×
per task and cutting tokens 43 %, with no detectable effect on cost. Four long
tasks at 17 steps mean is not enough to find the length at which an
uncompacted context stops fitting, which is where compaction should start to
matter. The crop-ageing TTL was fixed at N=3 throughout and never swept.

**Uncovered changed regions are not detected** (DECISIONS D4.8). Arm C's
forced-crop rule fires on visual roles and on a failed bbox-honesty check, but
not on "a region changed with no tree element behind it". I hit this directly:
the first version of the vdelta tasks used plain `<div>`s, which Chromium does
not expose, and *neither* arm saw the rows. Adding ARIA roles made them
visible. Real applications with unexposed custom controls would blind both
tool arms in a way this suite now avoids by construction.

**Single environment, single model, single session.** One container, one
model, one provider, one day. Nothing here speaks to Electron or Qt
applications, other models, or applications with a poorer accessibility tree.
The instrumentation-coverage question that governs the whole economics of the
approach (§6.3 of the design document) was not measured at all.

---

## 5. What I would do next, in order

1. **Extend the verifier controls to the six browser tasks.**
   `tests/test_verifiers.py` now builds each task's correct end state and
   asserts the verifier accepts it, and asserts an untouched workspace is
   rejected — the guard whose absence let `calc_total` ship an unsatisfiable
   check that read as a hard task through two versions of this report
   (DECISIONS D6.1). It covers the twelve verifiers that read the filesystem.
   The six that read live page state over CDP are still uncovered, and two of
   them carry the sharpest result in the experiment.
2. **Measure how often the vdelta case actually occurs** in real task
   distributions. It is the whole justification for crop-priority and this
   experiment shows only that it exists, not that it is common.
3. **Exercise compaction and crop-ageing** on tasks of 30+ steps. The tool
   arms' one clear weakness — 4× total tokens — is exactly what these are for,
   and neither ran.
4. **Sweep the enforced budget across more than one ceiling**, and over the
   full task suite rather than the 9-task subset §2.4(d) used.
5. **Implement uncovered-region detection**, so that a changed region with no
   tree element behind it produces a crop instead of silence.
6. **Raise N to 10+ per cell and pin temperature** before treating any pooled
   success-rate comparison as meaningful.
7. **Measure instrumentation coverage** across a real application catalogue,
   including Electron with and without the accessibility flag.

---

## 6. Summary

The observation layer delivers its mechanical claims in full: visual grounding
failure becomes impossible rather than rare, pixel traffic falls ~6.5×, and
the prefix cache survives (91 % vs 21.5 %), worth ~366 ms/step.

Its economics are conditional, and the condition is whether budget binds.
Given the same enforced budget of 8,000 fresh tokens per task, crop-priority
finished 27/27 tasks while the baseline finished 17/27 and ran dry in 16 of
them. With tokens unconstrained the two are indistinguishable, and the
baseline uses 4× fewer raw tokens, because an append-only context that is
never compacted costs more to re-read than a short context costs to rebuild.
If your agent is budget-limited, the tool is the better loop; if it is not,
this experiment gives you no reason to switch.

The two structural findings are cleaner than the headline. **The advantage
comes from sending only what changed, not from having the element table**: arm
D has the table without the deltas and costs 3.1× more per completed task than
arm B, making it the worst option tested. And **crops are worth their tokens**:
they cost 3.4 % more image tokens across the suite, where they rarely
fire, and they are the difference between 8/8 and 1/8 when what changed is
visible but not describable.
